package Gene;

/**
 * Represents an exception for the Gene chatbot.
 */
public class GeneException extends Exception {

    /**
     * Construct a new GeneException object with the error message.
     * @param message The error message of the exception.
     */
    public GeneException(String message) {
        super(message);
    }
}
