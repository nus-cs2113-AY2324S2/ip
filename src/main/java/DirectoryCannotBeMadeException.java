/** Exception if directory cannot be made in local folder */
public class DirectoryCannotBeMadeException extends Exception{
    public DirectoryCannotBeMadeException (String message) {
        super(message);
    }
}
